spring.application.name=hoian-cooking

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/hoian_cooking
spring.datasource.username=postgres
spring.datasource.password=12341234
spring.datasource.driver-class-name=org.postgresql.Driver

# Liquibase Configuration
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml
spring.liquibase.enabled=true

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:ds6fxaiqd}
cloudinary.api-key=${CLOUDINARY_API_KEY:131272294536855}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:8yOP2BkLtGeYFjULYEk59IWjZrA}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mail Configuration (SMTP)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:thinhnguyenndt248@gmail.com}
spring.mail.password=${MAIL_PASSWORD:etqe ixfe hgcb tyak}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Server Hosting Billing Job Configuration
# Cron: 8:00 PM (20:00) ngày 19 hàng tháng
app.billing.enabled=${BILLING_ENABLED:true}
app.billing.cron=${BILLING_CRON:0 0 18 19 * *}
app.billing.client-email=${BILLING_CLIENT_EMAIL:ly.win.1108@gmail.com}
app.billing.from-email=${BILLING_FROM_EMAIL:noreply@hoiancookingclass.vn}
app.billing.sender-name=${BILLING_SENDER_NAME:Hoi An Cooking - Hosting Management}
app.billing.amount-usd=${BILLING_AMOUNT_USD:15}
app.billing.amount-vnd=${BILLING_AMOUNT_VND:393.000 VND}
app.billing.bank-name=${BILLING_BANK_NAME:MBBank}
app.billing.bank-account-number=${BILLING_BANK_ACCOUNT_NUMBER:0703224025}
app.billing.bank-account-owner=${BILLING_BANK_ACCOUNT_OWNER:NGUYEN DUC THINH}
app.billing.billing-day=${BILLING_DAY:19}
app.billing.qr-image-path=${BILLING_QR_IMAGE_PATH:images/QR.jpg}

